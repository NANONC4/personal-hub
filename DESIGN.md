---
name: NANONC4 Personal Hub
description: พอร์ตโฟลิโอนักพัฒนาสายครีเอทีฟ — โลกพิกเซลอาร์ตกลางดึกที่เล่นได้จริง
colors:
  late-night-pink: "#f472b6"
  late-night-pink-deep: "#ec4899"
  lamplight-amber: "#fbbf24"
  lamplight-amber-pale: "#fef08a"
  signal-sky: "#38bdf8"
  dusk-indigo: "#818cf8"
  dusk-violet: "#c084fc"
  midnight-slate: "#0f172a"
  midnight-deep: "#020617"
  night-surface: "#1e293b"
  night-edge: "#334155"
  muted-slate: "#94a3b8"
  paper-white: "#f8fafc"
typography:
  display:
    fontFamily: "Inter, system-ui, sans-serif"
    fontSize: "clamp(3rem, 8vw, 6rem)"
    fontWeight: 900
    lineHeight: 1
    letterSpacing: "-0.05em"
  headline:
    fontFamily: "Silkscreen, monospace"
    fontSize: "clamp(1.875rem, 5vw, 3rem)"
    fontWeight: 400
    lineHeight: 1.1
    letterSpacing: "0.05em"
  title:
    fontFamily: "Silkscreen, monospace"
    fontSize: "1.25rem"
    fontWeight: 400
    lineHeight: 1.3
    letterSpacing: "0.05em"
  body:
    fontFamily: "Inter, system-ui, sans-serif"
    fontSize: "1.125rem"
    fontWeight: 400
    lineHeight: 1.7
    letterSpacing: "normal"
  label:
    fontFamily: "Silkscreen, monospace"
    fontSize: "0.625rem"
    fontWeight: 400
    lineHeight: 1
    letterSpacing: "0.2em"
rounded:
  sm: "0.25rem"
  md: "0.5rem"
  lg: "0.75rem"
  xl: "1rem"
  card: "2rem"
  full: "9999px"
spacing:
  xs: "0.5rem"
  sm: "1rem"
  md: "1.5rem"
  lg: "3rem"
  section: "6rem"
components:
  button-primary:
    backgroundColor: "{colors.midnight-slate}"
    textColor: "{colors.paper-white}"
    typography: "{typography.label}"
    rounded: "{rounded.md}"
    padding: "1rem 2rem"
    width: "12rem"
  button-primary-hover:
    backgroundColor: "{colors.late-night-pink-deep}"
    textColor: "{colors.paper-white}"
  chip-filter:
    backgroundColor: "transparent"
    textColor: "#64748b"
    typography: "{typography.label}"
    rounded: "{rounded.sm}"
    padding: "0.5rem 1.5rem"
  chip-filter-active:
    backgroundColor: "rgba(49, 46, 129, 0.3)"
    textColor: "#c7d2fe"
  card-project:
    backgroundColor: "#050810"
    textColor: "{colors.paper-white}"
    rounded: "{rounded.card}"
    padding: "1.5rem"
  card-bento:
    backgroundColor: "rgba(15, 23, 42, 0.4)"
    textColor: "{colors.paper-white}"
    rounded: "{rounded.card}"
    padding: "2rem"
  nav-link:
    textColor: "{colors.muted-slate}"
    typography: "{typography.label}"
    padding: "0.25rem 0.5rem"
  nav-link-active:
    textColor: "{colors.late-night-pink}"
---

# Design System: NANONC4 Personal Hub

## Overview

**Creative North Star: "The 2 A.M. Window"**

ห้องเงียบๆ ตอนตีสอง ไฟในห้องปิดหมดแล้ว เหลือแค่แสงจากจอกับไฟเมืองนอกหน้าต่าง — นั่นคือความรู้สึกที่ทั้งเว็บนี้ต้องให้ พื้นหลังเป็นสีกรมท่ามืดสนิทเกือบตลอด ตัวอักษรและองค์ประกอบลอยอยู่บนความมืดนั้นแบบไม่แย่งกันพูด สีสว่างมีน้อยและมาเป็นจุดๆ เหมือนไฟที่ยังเปิดอยู่ไม่กี่ดวง

โลกนี้ไม่ใช่ "อาร์เคด" ถึงจะมีเกมเล่นได้อยู่ในหน้าเว็บก็ตาม เกมและพิกเซลอาร์ตเป็น**ของเล่นในห้องที่เงียบ** ไม่ใช่ตัวกำหนดอารมณ์ทั้งห้อง งานที่เพิ่มเข้ามาใหม่ต้องเริ่มจากความสงบก่อน แล้วค่อยเลือกจุดที่จะขี้เล่นอย่างตั้งใจ ความเป็นพิกเซลอาร์ตอยู่ในรายละเอียดที่วาดขึ้นมาเองด้วยโค้ด (ดาว พระจันทร์ ไอคอน กรอบหน้าต่าง) ไม่ใช่ในการตะโกน

**สิ่งที่ยืนยันว่าไม่เอา:** หน้าตาแบบ portfolio dev ทั่วไป — การ์ดสี่เหลี่ยมเท่ากันหมดเรียงเป็นตาราง ไอคอนไลบรารีสำเร็จรูป พื้นขาวเทา ฟอนต์ระบบ ถ้างานใหม่ดูแทนที่ด้วยเว็บใครก็ได้ แปลว่าหลุดโลกนี้แล้ว

**Key Characteristics:**
- มืดเป็นค่าตั้งต้น ไม่มีโหมดสว่าง
- แบนเรียบเป็นหลัก ความลึกมาจากการไล่เฉดพื้นผิว ไม่ใช่เงา
- ตัวพิกเซล (Silkscreen) เป็นเสียงของหัวข้อและป้าย ตัว Inter เป็นเสียงของเนื้อความ
- สีสดใช้น้อยและมีหน้าที่ชัด ไม่โปรยเพื่อความสวย
- ทุกอย่างที่ขยับ ขยับช้าและมีเหตุผล
- องค์ประกอบ UI เบา โปร่ง หลีกทางให้เนื้อหานำ

## Colors

จานสีคือ "กลางคืนในเมือง": พื้นกรมท่าลึกหลายชั้น ตัดด้วยแสงไม่กี่ดวง — ชมพูคือสัญญาณ อำพันคือความอบอุ่น ฟ้า/ม่วงคือบรรยากาศ

### Primary
- **Late-Night Pink** (`#f472b6`): สีประจำแบรนด์และสีของ "สิ่งที่กดได้/สิ่งที่กำลังทำงานอยู่" ใช้กับ `NC4` ในโลโก้ เมนูที่ active (พร้อมขีดใต้) ขอบการ์ดตอน hover วงแหวน focus และคำที่ต้องเน้นในหัวข้อ
- **Late-Night Pink Deep** (`#ec4899`): เวอร์ชันเข้มสำหรับพื้นเต็ม เช่น พื้นปุ่มตอน hover ที่กวาดเข้ามาทับ

### Secondary
- **Signal Sky** (`#38bdf8`): คู่หูของชมพู ใช้กับคำที่สอง (`Developer`) กรอบหน้าต่างสไตล์ aesthetic และการ์ด bento ตอน hover เป็นสีของ "ระบบ/เทคนิค"
- **Dusk Indigo** (`#818cf8`): สีของตัวคั่นและตัวกรอง — ป้ายหมวดหมู่ที่เลือกอยู่ เส้นแบ่งโซน หัวข้อ CATEGORY

### Tertiary
- **Dusk Violet** (`#c084fc`): ใช้เป็นสำเนียงเสริมในกรอบรูปและเงาทึบสี ปรากฏน้อยที่สุดในสามสี
- **Lamplight Amber** (`#fbbf24`) และ **Lamplight Amber Pale** (`#fef08a`): แสงอุ่นดวงเดียวในจานสีเย็น สงวนไว้สำหรับ "สิ่งที่เปล่งแสงในความมืด" — พระจันทร์ ดาว ไฟหน้าต่างตึก หิ่งห้อย แสงโคมไฟ ห้ามเอาไปใช้เป็นสี UI ทั่วไป

### Neutral
- **Midnight Slate** (`#0f172a`): พื้นหลังหลักของทั้งเว็บ (`--background`) เป็นระดับ 0 ที่ทุกอย่างวางอยู่บน
- **Midnight Deep** (`#020617`): ชั้นที่ลึกกว่าพื้น — พื้นการ์ดที่จมลงไป ท้องฟ้าหลังตึก และเป็นสีของเงาทึบเกือบทุกครั้ง
- **Night Surface** (`#1e293b`): พื้นผิวที่ยกขึ้นมาหนึ่งชั้น — pill, แถบเครื่องมือ, พื้นแท็ก
- **Night Edge** (`#334155`): เส้นขอบและตัวคั่นที่มองเห็นได้ชัดกว่า border ปกติ
- **Muted Slate** (`#94a3b8`): เนื้อความรอง คำอธิบาย เมนูที่ไม่ active
- **Paper White** (`#f8fafc`): ตัวอักษรหลัก (`--foreground`) ใช้เต็มความสว่างเฉพาะหัวข้อและข้อความสำคัญ

**กฎ "แสงไม่กี่ดวง".** สีสด (ชมพู ฟ้า ม่วง อำพัน) รวมกันต้องไม่เกิน ~10% ของพื้นที่หน้าจอใดหน้าจอหนึ่ง ความหายากคือเหตุผลที่มันได้ผล ถ้าหน้าไหนดูสีเยอะ ให้ลดจำนวนดวง ไม่ใช่ลดความสด

**กฎ "อำพันคือแสง ไม่ใช่สี".** `lamplight-amber` ใช้ได้เฉพาะกับสิ่งที่กำลังเปล่งแสงในฉากกลางคืนเท่านั้น ห้ามใช้เป็นสีปุ่ม สีป้าย หรือสีเน้นข้อความ

**ธีมเฉพาะโปรเจกต์.** `data/projects.ts` มีฟิลด์ `theme` (`light` / `dark` / `blue` / `horror`) ที่ override สีเฉพาะภายในโซนของโปรเจกต์นั้น เช่นชุดสีแดงเลือด (`#4a0d0d`, `#200909`) ของ Rules of Horror สีเหล่านี้เป็นของโปรเจกต์ ไม่ใช่ของระบบ — ห้ามดึงออกมาใช้ที่อื่น

## Typography

**Display Font:** Inter (fallback: system-ui, sans-serif)
**Pixel Font:** Silkscreen — ตัวแปร `--font-pixel` น้ำหนักเดียวคือ 400
**Label/Mono Font:** ฟอนต์ mono ของระบบ ใช้กับป้ายที่ไม่ต้องการความเป็นพิกเซล

**Character:** เว็บนี้พูดสองเสียง — Inter หนามากๆ สำหรับชื่อและเนื้อความ (อ่านง่าย ทันสมัย) กับ Silkscreen สำหรับหัวข้อและป้าย (เป็นตัวจริงของโลกพิกเซล) ความต่างของสองเสียงนี้คือจังหวะของทั้งเว็บ อย่าเอาไปผสมกันในบรรทัดเดียว

### Hierarchy
- **Display** (Inter, 900, `clamp(3rem, 8vw, 6rem)`, line-height 1, tracking `-0.05em`): ชื่อในหน้าแรกเท่านั้น "Hi, I'm Dia."
- **Headline** (Silkscreen, 400, `clamp(1.875rem, 5vw, 3rem)`, tracking `0.05em`, ตัวพิมพ์ใหญ่): หัวข้อโซนและคำโปรยพิกเซล เช่น `CREATIVE DEVELOPER`, `PORTFOLIO`
- **Title** (Silkscreen, 400, `1.25rem`, uppercase): ชื่อโปรเจกต์บนการ์ด ชื่อหัวข้อย่อย
- **Body** (Inter, 400, `1.125rem`, line-height 1.7): เนื้อความไทย ใช้สี `muted-slate` เป็นค่าตั้งต้น กว้างไม่เกิน `max-w-2xl` (~65–75 ตัวอักษร)
- **Label** (Silkscreen หรือ mono, 400, `0.625rem`–`0.75rem`, tracking `0.2em`–`0.3em`, ตัวพิมพ์ใหญ่): เมนู ป้ายหมวดหมู่ แท็ก stack คำกำกับเล็กๆ — นี่คือลายเซ็นที่ใช้บ่อยที่สุดในเว็บ

**กฎ "ตัวเล็กต้องห่าง".** ทุกครั้งที่ใช้ตัวอักษรต่ำกว่า `0.875rem` ต้องมี `letter-spacing` อย่างน้อย `0.2em` และเป็นตัวพิมพ์ใหญ่ ตัวพิกเซลเล็กๆ ที่ไม่ยืดระยะจะอ่านไม่ออก

**กฎ "ชื่อเท่านั้นที่ไล่สี".** ตัวอักษรไล่เฉดสี (`animate-gradient-text` ฟ้า→ชมพู→ม่วง) สงวนไว้ให้ชื่อในหน้าแรกดวงเดียวในทั้งเว็บ หัวข้ออื่นเน้นด้วยขนาดและน้ำหนัก ไม่ใช่ด้วยการไล่สี

## Layout

คอนเทนเนอร์หลักคือ `max-w-6xl` สำหรับหน้าที่เน้นการอ่าน และ `max-w-7xl` สำหรับตารางผลงาน ขอบข้าง `px-6` บนมือถือ ขยับเป็น `px-12` ตั้งแต่ `md` ขึ้นไป

จังหวะแนวตั้งเป็นตัวกำหนดความสงบ: โซนหลักเว้น `py-20` บนมือถือ และ `py-24`–`py-32` บนจอใหญ่ ระยะห่างภายในกลุ่มใช้ `gap-2`–`gap-4` ส่วนระยะระหว่างกลุ่มใช้ `mb-10`–`mb-12` — เกาะกลุ่มให้ชิด แยกกลุ่มให้ห่างชัด

ตารางใช้สองแบบ: **bento** (`auto-rows-[22rem]`, 1 คอลัมน์บนมือถือ → 3 คอลัมน์ที่ `md`, `gap-6`) สำหรับบริการ และ **ตารางไม่สม่ำเสมอ** (`lg:grid-cols-12` แล้วให้การ์ดสลับกินพื้นที่ 7 กับ 5 คอลัมน์) สำหรับผลงาน — จงใจไม่ให้การ์ดเท่ากันหมด

หน้าแรกมี hero สูง `min-h-[90vh]` และมีชั้นพื้นหลังแบบ `fixed inset-0` (ท้องฟ้าพิกเซล) ที่อยู่นิ่งตลอดขณะเลื่อน เนื้อหาทั้งหมดวางบน `relative z-10` เหนือชั้นนั้น

เบรกพอยต์ตามค่าเริ่มต้นของ Tailwind: `sm 640` / `md 768` / `lg 1024` / `xl 1280`

## Elevation & Depth

**ระบบนี้แบนเป็นหลัก** ความลึกเกือบทั้งหมดมาจากการไล่เฉดพื้นผิว ไม่ใช่เงา: `midnight-deep` (จมที่สุด) → `midnight-slate` (พื้น) → `night-surface` (ยกขึ้น) ประกอบกับเส้นขอบบางๆ แบบโปร่งแสง (`border-slate-800`, `border-white/10`) หน้าจอหนึ่งจะมีไม่เกินสามระดับความลึก

เงาทึบไม่เบลอ (`4px 4px 0`) **ไม่ใช่ระบบมิติหลัก** แต่เป็นสำเนียงขี้เล่นที่ใส่เฉพาะจุด — กรอบรูปสไตล์เรโทร แถบเครื่องมือกรองผลงาน ป้ายในโซนเกม เมื่อใช้ ให้ใช้เต็มที่ไปเลย และให้เงาเป็นสีจากจานสีจริง (`midnight-deep`, `night-surface`, `dusk-violet`) ไม่ใช่สีดำลอยๆ

แสงเรืองสี (glow) เป็นการตอบสนอง ไม่ใช่การตกแต่ง — ใช้ตอน hover เท่านั้น

### Shadow Vocabulary
- **Block Small** (`box-shadow: 2px 2px 0 0 #020617`): ป้ายเล็ก แถบเครื่องมือ ปุ่มในโซนขี้เล่น
- **Block Medium** (`box-shadow: 4px 4px 0 0 #1e293b`): กรอบรูปเรโทร การ์ดที่ต้องการความเป็นสติกเกอร์
- **Block Accent** (`box-shadow: 8px 8px 0 0 #c084fc`): ใช้ได้ครั้งเดียวต่อหนึ่งโซน สำหรับองค์ประกอบที่ต้องเป็นพระเอก
- **Hover Glow** (`box-shadow: 0 0 40px rgba(236,72,153,0.15)`): การ์ดผลงานตอน hover — เรืองแสงเบาๆ ไม่ใช่เงาแข็ง
- **Pixel Text Shadow** (`drop-shadow: 1px 1px 0 rgba(0,0,0,0.8)`): ตัวพิกเซลบนพื้นที่มีลาย ให้ตัวอักษรไม่จมไปกับพื้นหลัง

**กฎ "แบนก่อน ค่อยยก".** พื้นผิวเริ่มจากแบนเสมอ เงาจะปรากฏก็ต่อเมื่อมีเหตุผล — hover, การเป็นของเล่น, หรือความจำเป็นในการอ่าน ไม่มีเงา "เผื่อสวย"

## Shapes

ภาษารูปทรงมีสามระดับที่ชัดเจน และเลือกจาก**หน้าที่** ไม่ใช่จากขนาด:

- **โค้งเต็ม** (`rounded-full`) — ใช้มากที่สุดในเว็บ สำหรับสิ่งที่เป็น "เม็ด": pill ป้ายกำกับ ปุ่มไอคอน จุดสถานะ อวตาร
- **โค้งกลาง** (`rounded-lg` `0.5rem` / `rounded-xl` `1rem`) — ปุ่ม แถบเครื่องมือ กล่องเล็ก
- **โค้งใหญ่** (`rounded-[2rem]` / `rounded-[2.5rem]`) — การ์ดผลงานและการ์ด bento เท่านั้น ความโค้งที่ใหญ่ผิดปกตินี้คือสิ่งที่ทำให้การ์ดดูนุ่มขึ้นบนพื้นมืด

ตรงข้ามกันคือ **มุมฉากสนิท** (`rounded-none`) ซึ่งสงวนไว้ให้พิกเซลอาร์ตล้วน — กรอบหน้าต่าง ตัวเกม ไอคอนพิกเซล ของพวกนี้ห้ามโค้ง เพราะความคมคือตัวตนของมัน

เส้นขอบเป็นค่าตั้งต้น `1px` แบบโปร่งแสง (`border-slate-800`, `border-white/10`) ขยับเป็น `2px` เมื่อเป็นการ์ด และ `4px` เฉพาะกรอบสไตล์เรโทรที่ตั้งใจให้หนา

พิกเซลอาร์ตทุกชิ้นต้องมี `image-rendering: pixelated` เพื่อไม่ให้ขอบเบลอตอนขยาย

## Components

องค์ประกอบทั้งหมดยึดหลักเดียวกัน: **เบา โปร่ง หลีกทาง** — UI ต้องไม่แย่งความสนใจจากเนื้อหา ความน่ากดมาจากการตอบสนองตอนโดน ไม่ใช่จากน้ำหนักตอนอยู่เฉยๆ

### Buttons
- **Shape:** โค้งกลาง (`0.5rem`) กว้างคงที่ `12rem` สำหรับปุ่มหลัก
- **Primary:** พื้นเดียวกับพื้นหลัง (`midnight-slate`) ขอบ `night-edge` ตัวอักษร mono ตัวหนา ตัวพิมพ์ใหญ่ `tracking-widest` — ปุ่มดูเบามากตอนอยู่เฉย
- **Hover:** ขอบเปลี่ยนเป็น `late-night-pink` พร้อมจุดชมพูเล็กๆ ที่ขยายกวาดเต็มปุ่ม และข้อความเดิมเลื่อนออกขวาแล้วมีข้อความเดิมพร้อมลูกศรเลื่อนเข้ามาแทน (300ms) — นี่คือลายเซ็นของปุ่มในเว็บนี้
- **Ghost:** ข้อความล้วนสี `muted-slate` เปลี่ยนเป็น `paper-white` ตอน hover ไม่มีพื้น ไม่มีขอบ

### Chips
- **Style:** ตัวพิกเซลเล็ก (`0.625rem`) `tracking-widest` ตัวพิมพ์ใหญ่ ไม่มีพื้นตอนไม่ได้เลือก สีข้อความ `#64748b`
- **State:** ตอนเลือก พื้นเป็นคราม `rgba(49,46,129,0.3)` ขอบครามจางๆ มีเงาด้านใน (`inset 0 1px 2px`) ตัวหนังสือสว่างขึ้น ตัวบ่งชี้เลื่อนตามด้วย `layoutId` แบบสปริง (stiffness 400, damping 30)
- แถบที่ครอบ chip ทั้งหมดมีพื้น `midnight-slate` ขอบล่างหนา 4px และเงาทึบเล็ก — เป็นหนึ่งในจุดที่ "ขี้เล่นได้"

### Cards / Containers
- **Corner Style:** โค้งใหญ่ (`2rem`)
- **Background:** การ์ดผลงานใช้ `#050810` (จมกว่าพื้น) การ์ด bento ใช้ `rgba(15,23,42,0.4)` แบบโปร่ง
- **Shadow Strategy:** แบนตอนอยู่เฉย → ตอน hover ขอบเปลี่ยนเป็นสีสำเนียง (ชมพูสำหรับผลงาน ฟ้าสำหรับบริการ) พร้อม glow เบาๆ และยกขึ้น `-translate-y-4`
- **Border:** `2px` สี `night-surface` เป็นค่าตั้งต้น
- **Internal Padding:** `1.5rem` สำหรับการ์ดผลงาน `2rem` สำหรับ bento

### Navigation
- แถบบนคงที่ พื้นโปร่งใสตอนอยู่บนสุด → เลื่อนลงเกิน 20px เปลี่ยนเป็น `bg-slate-950/80` พร้อม `backdrop-blur-md` ขอบล่าง และหดความสูงลง (300ms)
- ลิงก์เป็นตัว mono เล็ก ตัวพิมพ์ใหญ่ `tracking-widest` สี `muted-slate` → `paper-white` ตอน hover
- หน้าที่ active เป็น `late-night-pink` พร้อมขีดใต้ที่เลื่อนตามด้วย `layoutId` สปริง (stiffness 300, damping 30)
- บนมือถือเป็นแฮมเบอร์เกอร์สามขีดที่แปลงเป็นกากบาทสีชมพู เปิดเป็นเลเยอร์เต็มจอ เมนูเป็นตัวพิกเซล `3xl` ไล่กันเข้ามาทีละอัน

### PixelSky (Signature Component)
ชั้นท้องฟ้าพิกเซลบน `<canvas>` ที่วางแบบ `fixed inset-0` อยู่หลังทุกหน้า มีดาวกะพริบและดาวตก และมีโหมด `cozy` เพิ่มเมืองกลางคืน แสงขอบฟ้า หิ่งห้อย และหมอกบางๆ สำหรับหน้าแรก ทุกอย่างเคลื่อนช้าและอยู่ในระดับความทึบต่ำ — มันคือ "นอกหน้าต่าง" ของทั้งเว็บ ห้ามให้มันดังกว่าเนื้อหาที่วางทับ

### Retro Window Frames (Signature Component)
กรอบรูปสไตล์หน้าต่างที่ครอบภาพในโซนโปรเจกต์ (`AestheticWindowFrame`, `SunsetWindowFrame`, `HorrorWindowFrame`) แต่ละอันมีชุดสีของตัวเองตาม `theme` ของโปรเจกต์ ใช้ขอบหนา 4px เงาทึบ และองค์ประกอบพิกเซลลอยรอบๆ — นี่คือจุดที่ระบบอนุญาตให้ขี้เล่นเต็มที่

## Do's and Don'ts

### Do:
- **Do** เริ่มทุกหน้าจากพื้น `midnight-slate` แล้วเพิ่มความลึกด้วยการไล่เฉด (`midnight-deep` / `night-surface`) ก่อนคิดถึงเงา
- **Do** ใช้ Silkscreen กับหัวข้อและป้ายทุกครั้ง และ Inter กับเนื้อความทุกครั้ง — สองเสียงนี้ห้ามสลับหน้าที่กัน
- **Do** ยืดระยะตัวอักษร (`tracking-widest` ขึ้นไป) และใช้ตัวพิมพ์ใหญ่กับทุกป้ายที่เล็กกว่า `0.875rem`
- **Do** ให้สีสดทำหน้าที่เดียว: ชมพู = สิ่งที่กดได้/active, ฟ้า = ระบบและเทคนิค, คราม = ตัวกรองและตัวคั่น, อำพัน = แสงในฉากกลางคืน
- **Do** ให้ทุกแอนิเมชันวนซ้ำเคลื่อนช้า (6 วินาทีขึ้นไป) และมีความทึบต่ำ เมื่ออยู่หลังข้อความที่ต้องอ่าน
- **Do** ใส่ `image-rendering: pixelated` ให้พิกเซลอาร์ตทุกชิ้น และใช้ `rounded-none` กับมันเสมอ
- **Do** เคารพ `prefers-reduced-motion` ในทุกองค์ประกอบที่เคลื่อนไหว — ปิดลูปทั้งหมด เหลือแต่สถานะนิ่งที่อ่านได้

### Don't:
- **Don't** เพิ่มโหมดสว่าง เว็บนี้มืดอย่างเดียวโดยตั้งใจ
- **Don't** ใช้ `lamplight-amber` เป็นสี UI (ปุ่ม ป้าย ข้อความเน้น) — มันเป็นสีของแสงเท่านั้น
- **Don't** ใส่ตัวอักษรไล่เฉดสีเพิ่มที่ไหนอีก นอกจากชื่อในหน้าแรกที่มีอยู่แล้ว
- **Don't** ใช้เงาทึบ (`4px 4px 0`) เป็นค่าตั้งต้นของการ์ดหรือปุ่มทั่วไป — มันสงวนไว้ให้จุดที่ตั้งใจขี้เล่น
- **Don't** ทำให้การ์ดในตารางผลงานเท่ากันหมด ความไม่สม่ำเสมอ (7/5 คอลัมน์สลับกัน) คือสิ่งที่ทำให้ไม่ดูเป็นเทมเพลต
- **Don't** ดึงสีธีมเฉพาะโปรเจกต์ (เช่นแดงเลือดของ Rules of Horror) ออกมาใช้นอกโซนของโปรเจกต์นั้น
- **Don't** ซ้อนป้ายกำกับเล็กหลายชั้นเหนือหัวข้อเดียว หนึ่งป้ายพอ แล้วปล่อยให้หัวข้อทำงานเอง
- **Don't** ใช้อีโมจิแทนไอคอน ไอคอนต้องเป็นพิกเซลที่วาดเอง (`PixelIcons`) หรือ lucide-react ที่มีน้ำหนักเส้นสม่ำเสมอ
